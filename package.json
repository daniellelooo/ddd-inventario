{
  "name": "development-scripts",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && dotnet run --project InventarioDDD.API",
    "frontend": "cd frontend && npm start",
    "install": "cd frontend && npm install",
    "build": "cd frontend && npm run build",
    "docs": "echo Open backend/docs/ folder to view DDD documentation"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}